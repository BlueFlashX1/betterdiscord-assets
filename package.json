{
  "name": "betterdiscord-dev",
  "version": "1.0.0",
  "description": "BetterDiscord plugin and theme development environment",
  "scripts": {
    "build": "node scripts/build-plugin.js --all",
    "build:plugin": "node scripts/build-plugin.js",
    "build:changed": "node scripts/build-plugin.js --changed",
    "watch": "node scripts/build-plugin.js --watch",
    "build:all": "node scripts/build-plugin.js --all",
    "watch:all": "node scripts/build-plugin.js --all --watch",
    "link:plugin": "node scripts/link-plugin.js && console.log('Plugin linked!')",
    "link:theme": "node scripts/link-theme.js",
    "harness:harmony": "node scripts/plugin-harmony-harness.js",
    "verify:shadowaway-sync": "scripts/verify-shadowawaybridge-sync.sh",
    "check-drift": "node scripts/check-class-drift.mjs"
  },
  "keywords": [
    "betterdiscord",
    "discord",
    "plugin",
    "theme"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chokidar": "^3.5.3",
    "esbuild": "^0.27.3",
    "fs-extra": "^11.2.0"
  }
}
