# Full Plugin Audit — 2026-03-13

**Scope**: 21 active plugins + shared/ (excluding CSSPicker)
**Total findings**: ~180
**Agents**: 6 parallel auditors

---

## CRITICAL Findings (15)

| # | Plugin | File | Issue | Fix |
|---|--------|------|-------|-----|
| 1 | SoloLevelingStats | lifecycle.js:43 | `checkDailyQuests` throttle binding — method doesn't exist (should be `checkDailyReset`) | Rename to `checkDailyReset` |
| 2 | SoloLevelingStats | settings-store.js | Save version hardcoded `2.5.0` but manifest is `3.0.5` — corrupts migration gates | Use `this.meta?.version` |
| 3 | Dungeons | combat-damage-calc.js:2-3 | Dead `const Dungeons` shadows class name | Delete lines 2-3 |
| 4 | Dungeons | runtime-visibility.js:1 | `_PluginUtils` load failure swallowed silently — NavigationBus never fires | Add warn-once on null |
| 5 | Dungeons | index.js mixin order | `settings-persistence` → `stats-integration` order dependency undocumented, fragile | Add explicit comment |
| 6 | CriticalHit | pipeline.js:341-380 | `elementWithClass` computed in walk-up loop but never used | Remove dead walk-up block |
| 7 | CriticalHit | pipeline.js:336-383 | Inner try-catch re-throws without logging — misleads readers | Remove inner try-catch |
| 8 | ShadowArmy | extraction.js:503 | `userRank` parameter shadowed by inner `const userRank` | Rename inner to `resolvedRank` |
| 9 | ShadowSenses | index.js + senses-engine-utils.js | Duplicate `_fallbackToast` module-scope instances — split state | Create toast once in index.js |
| 10 | ShadowStep | index.js:429 | `_toast()` called with 3 args but `createToast()` only accepts 2 — duration silently dropped | Extend `createToast()` to accept duration |
| 11 | ShadowExchange | index.js:528-530 | `debugError` logs unconditionally — noisy in production for expected DM-channel errors | Gate behind debug mode for expected errors |
| 12 | ShadowExchange | (missing) | No `getSettingsPanel()` — users can't configure debug/animation settings | Create settings-panel.js |
| 13 | ShadowExchange | index.js:785,823,839 | Same 3-arg `_toast()` duration-dropped issue as ShadowStep | Same fix — extend createToast |
| 14 | LevelProgressBar | index.js:235 | `_lastMilestoneMask` dead — renamed to `_lastMilestoneCount` but mask logic may be wrong | Audit milestone detection logic |
| 15 | ShadowArmy | compression.js:254 | `lastNaturalGrowth` set to `extractedAt` after decompression — causes immediate growth grant | Add `lng` field to compressed format |

---

## HIGH Findings (40+)

### SoloLevelingStats
- `hp-mana.js` — `userRankHpLinearStep`/`userRankHpCurveStep` settings never initialized (NaN risk)
- `webpack-integration.js` — Redundant second `getCurrentUserIdFromStore()` call
- `activity-tracking.js:1` — Unused `dc = require("../shared/discord-classes")` import
- `progression-read-model.js` — `statLabels` defined twice (second shadows first)
- `calculation-bonuses.js` — `calculateChannelActivityBonus` always returns 2 (dead channel lookup)
- `criticalhit-integration.js` — `checkDailyReset` misplaced (belongs in quests.js)
- `achievements.js` — 4 shadow-power methods misplaced (belong in shadowarmy-integration.js)

### Dungeons
- `storage.js` — `_batchQueue`/`_batchTimer`/`_batchThreshold`/`_batchIntervalMs` never used
- `combat-shadow-support.js` — `initializeShadowHP` is pure async wrapper over sync method
- `combat-shadow-execution.js` — `processShadowAttacks` re-implements shadow filter instead of calling `getCombatReadyShadows`
- `combat-shadow-allocation.js` — Triplicate background interval calculation across 3 functions
- `combat-boss-mob.js` — Identical `resolveShadowMapKey`/`getShadowHpData` closures in 2 functions
- `spawn-wave-builders.js` — Mob-cap warning+timeout block copy-pasted twice
- `stats-integration.js` — Dual shadow count caches (`this.cache` + `this._shadowCountCache`)
- `combat-role-damage.js` — `buildShadowStats` fallback duplicated from combat-damage-calc.js

### CriticalHit
- `restoration.js` — `_createSimpleContentHash` duplicates `calculateContentHash` from id-extraction.js
- `settings-panel.js` — `perceptionInfo` state set via polling but never rendered
- `styling.js` — `pluginsFolder` computed but never consumed; font-type classifier duplicated
- `dom-helpers.js` — Silent failure if NavigationBus unavailable (no popstate fallback)

### ShadowArmy
- `watchers.js:379-391` — Race condition: duplicate `_memberListHealthCheck` intervals
- `army-stats.js:262-264` — Fallback stat distribution inflates zero-stat shadows
- `extraction.js` — Shadow record construction literal duplicated in 2 functions

### ShadowSenses
- Engine modules — Double-export pattern (plain-name exports never consumed)

### ShadowRecon
- `_Dispatcher` acquired but never used
- Dead exports: context-menu.js (5), target-intel.js (6), permissions.js (4), guild-visuals.js (5)

### ShadowStep
- `index.js:457-459` — `_ensureShadowPortalCoreApplied` re-called at runtime after module-load call

### ShadowPortalCore
- `index.js:614` — Reads `settings.debugMode` but ShadowExchange stores it as `settings.debug`

### Small Plugins
- HSLDockAutoHide — `_skillTreeRetryTimer` never declared in constructor
- LevelProgressBar — `_invokeRuntimeHelper` silently returns undefined when runtime absent
- SoloLevelingToasts — Mixed ESM import + CJS require in same file
- Stealth — `stopTyping` patched twice (direct + via `_patchFunctions`)
- SystemWindow — `styles.css` exists but is never imported (dead file)
- UserPanelDockMover — Hardcoded dock selector vs HSLDockAutoHide's 4-selector fallback

### shared/
- `debug.js` — `mixinDebug` exported, zero plugins import it
- `dispatcher.js` — `acquireDispatcher`/`pollForDispatcher` exported, zero plugins import them
- `settings.js` — Never imported by any src/ plugin

### ShadowAwayBridge
- `runtime.js` — Pre-built bundle checked into src/ (should be proper source)

---

## MEDIUM Findings (60+)

### Duplicate Patterns (extract to helpers)
- `createToast()` 3-arg pattern — extend shared/toast.js to accept duration
- `_getCreateRoot` React 18/17 dual-path — duplicated in ShadowSenses + SkillTree (use shared/react-dom.js)
- `shared-utils.js` structure — duplicated verbatim between ShadowSenses + SkillTree
- Portal transition CSS — identical in ShadowExchange + ShadowStep (move to ShadowPortalCore)
- `_getSkillTreeSkillLevel` — 3 copies across ShadowStep/Exchange/PortalCore
- Toast fallback init — duplicated across ChatNavArrows/HSLDockAutoHide/Stealth/others
- Poll-until pattern — duplicated across HSLDockAutoHide/HSLWheelBridge/ChatNavArrows
- Keyword normalization — duplicated in ShadowSenses components.js + deployment-manager.js
- Stat labels/metadata — duplicated across SoloLevelingStats files
- channelsVisited serialization — duplicated in SoloLevelingStats settings-store.js
- Background interval calc — triplicated in Dungeons combat files
- Boss stat validation range — duplicated in Dungeons restore-gc-toast + combat-boss-mob
- Font-type classifier — duplicated in CriticalHit styling.js
- ChatNavArrows scroller utils — duplicated across arrow-manager + dom-fallback
- Dock selectors — duplicated across HSLDockAutoHide + UserPanelDockMover
- Sort options — duplicated across TitleManager index.js + components.js

### Dead Code
- SoloLevelingStats: `getEffectiveShadowArmyBuffs` trivial alias, `_selectBestSettingsCandidate` wrapper
- Dungeons: `attemptAutoResurrection` superseded, `_killedMobs` unused, `_count` unused, `GuildStore.getGuild` result discarded
- CriticalHit: `COMBO_TIMEOUT_MS` unused, `_saveUserComboToStorage` no-op stub, `injectSettingsCSS` no-op
- ShadowExchange: `this.NavigationUtils` (no underscore) never read, `_startupRetryCount` never reset
- Stealth: `_recordSuppressed` writes timestamps never read
- TitleManager: `webpackModules.ChannelStore` fetched but never used
- UserPanelDockMover: `this.version` dead property

### Mismatches
- SoloLevelingStats: `per`/`luk` stat aliases undocumented, comment says 0.25 base but code uses 0.20
- Dungeons: `calculateAttacksInSpan` vs `calculateAttacksInTimeSpan` near-identical names, mob ID uses Date.now() vs counter inconsistently
- ShadowExchange: `_resetRuntimeState` doesn't reset `_startupRetryCount`, stale comment references `dc.sel.toolbar`
- ShadowPortalCore: `configuredDuration` computed but not used in actual timing
- SoloLevelingToasts: `this.debugMode` vs `this.settings.debugMode` — two separate sources
- Stealth: `status-setters.js` registers `getStatus` as a setter candidate (functional bug)
- RulersAuthority: Mixed ESM import + CJS require in same plugin

### Comment Cleanup
- SoloLevelingStats: 8 empty `#region agent log` stubs in rank-progression.js, empty region in notifications.js
- Dungeons: orphaned sprint reference comments
- CriticalHit: 4 placeholder SECTION blocks, 2 tombstone version comments
- ShadowStep: Inaccurate ToC comment block
- ShadowExchange: 26-line v2.1.0 migration history (migration is done)
- TitleManager: 76-line version history in source (belongs in CHANGELOG)
- ShadowAwayBridge: Misleading shim comment

### Legacy
- SoloLevelingStats: migrations run every load with no version gate
- ShadowArmy: v3 migration check duplicated inside function and in runDataMigrations
- CriticalHit: `getUserId` vs `getAuthorId` near-duplicate fiber traversal
- HSLDockAutoHide: `getCSS()` never called, `requireRevealReset` empty block

---

## LOW Findings (50+)

Omitted for brevity — includes minor style issues, redundant guards, undocumented aliases, decorative comments, and trivial naming inconsistencies.

---

## Cross-Plugin Patterns to Extract

| Pattern | Current Location(s) | Proposed shared/ Module |
|---------|---------------------|------------------------|
| `createToast(msg, type, duration)` | ShadowStep, ShadowExchange, ShadowSenses + 5 others | Extend `shared/toast.js` |
| `getCreateRoot()` React 18/17 | ShadowSenses, SkillTree, ShadowExchange | `shared/react-dom.js` (already exists, adopt it) |
| `_getSkillTreeSkillLevel(id)` | ShadowStep, ShadowExchange, ShadowPortalCore | `shared/skilltree.js` |
| Portal transition CSS | ShadowStep, ShadowExchange | Move to ShadowPortalCore |
| `pollUntil(fn, opts)` | HSLDockAutoHide, HSLWheelBridge, ChatNavArrows | `shared/poll-until.js` |
| `shared-utils.js` boilerplate | ShadowSenses, SkillTree | `shared/plugin-shared-utils.js` factory |
| Dock selectors array | HSLDockAutoHide, UserPanelDockMover | `shared/dock-selectors.js` |
| `STAT_LABELS` / stat metadata | SoloLevelingStats (3+ files) | `SoloLevelingStats/constants.js` |
| `SOLO_RANK_ORDER` | SkillTree index.js + skill-upgrade-methods.js | `SkillTree/data.js` |
| Scroller utils + arrow factory | ChatNavArrows (2 files) | `ChatNavArrows/scroller-utils.js` |

---

## Dead shared/ Modules (zero importers)

| Module | Status |
|--------|--------|
| `shared/debug.js` (`mixinDebug`) | Dead — no plugin imports it |
| `shared/dispatcher.js` | Dead — no plugin imports it |
| `shared/settings.js` | Dead — only bundled copy in ShadowAwayBridge |
| `shared/warn-once.js` `clearWarnings` | Dead unexported function |
| `shared/react-dom.js` `getLegacyReactDOM` | Dead export |
| `shared/discord-classes.js` `DEFS` | Dead export |

---

## Recommended Fix Order

### Phase 1 — Critical (do first)
1. SoloLevelingStats: `checkDailyQuests` → `checkDailyReset`
2. SoloLevelingStats: version `2.5.0` → `this.meta?.version`
3. shared/toast.js: extend `createToast` to accept duration param
4. ShadowArmy: rename inner `userRank` to `resolvedRank`
5. ShadowArmy: add `lng` field to compression format
6. Dungeons: delete dead `const Dungeons` in combat-damage-calc.js
7. CriticalHit: remove dead `elementWithClass` + useless re-throw
8. ShadowSenses: single toast instance
9. LevelProgressBar: audit `_lastMilestoneMask` vs `_lastMilestoneCount`

### Phase 2 — Dead code removal
1. Remove unused imports (`dc` in activity-tracking.js, etc.)
2. Remove dead exports from ShadowRecon (20 functions)
3. Remove dead shared/ modules (debug.js, dispatcher.js consumers or delete)
4. Delete SystemWindow/styles.css (unreachable)
5. Remove dead properties/methods across all plugins

### Phase 3 — Duplicate extraction
1. Portal transition CSS → ShadowPortalCore
2. `_getSkillTreeSkillLevel` → shared/skilltree.js
3. `_getCreateRoot` → shared/react-dom.js adoption
4. `shared-utils.js` → factory pattern
5. ChatNavArrows scroller-utils.js
6. Stat labels → SoloLevelingStats/constants.js

### Phase 4 — Comment/legacy cleanup
1. Remove empty #region stubs
2. Remove tombstone comments
3. Remove version history from source files
4. Trim decorative section dividers
5. Fix stale comments (0.25→0.20, dc.sel.toolbar, etc.)

### Phase 5 — Mismatch fixes
1. ShadowPortalCore `debugMode` vs `debug` setting name
2. Stealth `stopTyping` double-patch
3. Stealth `getStatus` registered as setter
4. SoloLevelingToasts dual debug mode sources
5. RulersAuthority ESM/CJS mix
6. Dungeons mob ID generation inconsistency
